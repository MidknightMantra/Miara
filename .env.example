# ─────────────────────────────────────────────
# 🌸 Miara 🌸 — Environment Configuration
# by MidKnightMantra × GPT-5 — 2025
# Universal configuration for VPS, Render, Heroku, and Docker Panel
# ─────────────────────────────────────────────

# ── 💫 BOT IDENTITY ──
BOT_NAME=Miara🌸
BIO=🌸 M I A R A 🌸

# ── 🧠 OWNER INFO ──
OWNER_NAME=MidKnightMantra
# Comma-separated WhatsApp numbers (no + or 00)
# Example: 254712345678,15551234567
OWNER_NUMBER=254105745317,yournumberhere

# ── ⚙️ CORE SETTINGS ──
PREFIX=.
PRIVATE_MODE=false              # true = only owner can use commands
LANGUAGE=en
REGION=Unknown
TIMEZONE=Africa/Nairobi          # Used for timestamps and emotional tone

# ── 🚀 BOOT BEHAVIOR ──
BOOT_MESSAGE=true                # Send startup message when connected
BOOT_TARGET=254105745317@s.whatsapp.net
SHOW_CONSOLE_REPORT=true         # Print status banner and system info on startup

# ── 🩵 STICKER DEFAULTS ──
STICKER_PACK_NAME=Miara Pack
STICKER_AUTHOR=MidKnightMantra🌸

# ── 📂 FILE PATHS ──
SESSION_PATH=./session
STORE_PATH=./src/database/baileys_store.json
DATABASE_PATH=./src/database/database.json

# ── 🔐 SECURITY ──
# Protects admin routes, backup endpoints, or panel access
ADMIN_KEY=YourSuperSecretKey123
SESSION_BACKUP_RETENTION_DAYS=5

# ── 🌍 EXTERNAL INTEGRATIONS ──
# Fill these if you plan to use AI or database features
MONGO_URI=
DATABASE_URL=
OPENAI_API_KEY=your-openai-key
GOOGLE_GEMINI_KEY=your-gemini-key
MISTRAL_KEY=your-mistral-key
HF_TOKEN=your-huggingface-token
OPENWEATHER_API_KEY=4902c0f2550f58298ad4146a92b65e10

# ── 🎛️ FEATURE TOGGLES ──
AUTO_STICKER=true
GREETING_MESSAGES=true
EMOTION_CAPTIONS=true

# ── 🖥️ DEPLOYMENT FLAGS ──
PANEL_MODE=false                 # Hide QR (for web pairing / panel mode)
QUIET_MODE=false                 # Suppress banner/log output
PORT=3000                        # Health server port (Render / Heroku friendly)

# ── 🌐 OPTIONAL ENVIRONMENT HINTS ──
# RENDER=true                    # uncomment if deploying to Render
# HEROKU=true                    # uncomment if deploying to Heroku
# DOCKER=true                    # uncomment if inside Docker container
# RAILWAY_STATIC_URL=            # auto-detected if on Railway
# CODESPACES=true                # auto-detected if in GitHub Codespaces
