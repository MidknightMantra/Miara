# ─────────────────────────────────────────────
# 🌸 Miara 🌸 — Environment Configuration
# by MidKnightMantra × GPT-5 — 2025
# Universal for VPS, Render, Heroku, or Panel
# ─────────────────────────────────────────────

# ── 💫 BOT INFO ──
BOT_NAME=Miara🌸
BIO=🌸 M I A R A 🌸

# ── 🧠 OWNER INFO ──
OWNER_NAME=MidKnightMantra
# Comma-separated WhatsApp numbers (no + or 00)
# Example: 254712345678,15551234567
OWNER_NUMBER=254105745317,yournumberhere

# ── ⚙️ CORE SETTINGS ──
PREFIX=.
PRIVATE_MODE=false              # true = only owner can run commands
LANGUAGE=en
REGION=Unknown
TIMEZONE=Africa/Nairobi          # Affects timestamps & greetings

# ── 🚀 BOOT BEHAVIOR ──
BOOT_MESSAGE=true                # Send a system message after connect
BOOT_TARGET=254105745317@s.whatsapp.net
SHOW_CONSOLE_REPORT=true         # Show system info in logs

# ── 🩵 STICKER DEFAULTS ──
STICKER_PACK_NAME=Miara Pack
STICKER_AUTHOR=MidKnightMantra🌸

# ── 📂 PATHS ──
SESSION_PATH=./session
STORE_PATH=./src/database/baileys_store.json
DATABASE_PATH=./src/database/database.json

# ── 🔐 SECURITY ──
ADMIN_KEY=YourSuperSecretKey123   # Protects admin routes (/sessions delete, /backup)
SESSION_BACKUP_RETENTION_DAYS=5

# ── 🌍 EXTERNAL INTEGRATIONS ──
MONGO_URI=
DATABASE_URL=
OPENAI_API_KEY=your-openai-key
GOOGLE_GEMINI_KEY=your-gemini-key
MISTRAL_KEY=your-mistral-key
HF_TOKEN=your-huggingface-token
OPENWEATHER_API_KEY=4902c0f2550f58298ad4146a92b65e10

# ── 🎛️ FEATURE TOGGLES ──
AUTO_STICKER=true
GREETING_MESSAGES=true
EMOTION_CAPTIONS=true

# ── 🖥️ DEPLOYMENT FLAGS ──
PANEL_MODE=false                 # Hide QR (for web pairing)
QUIET_MODE=false                 # Suppress banner/logs
PORT=3000                        # Health server port (for Render/Heroku)
