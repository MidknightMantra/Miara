# ─────────────────────────────────────────────
# 🌸 Miara 🌸 — .gitignore
# by MidKnightMantra × GPT-5 — 2025
# Secure, Cloud-Ready, and Developer Friendly
# ─────────────────────────────────────────────

# ── 🧩 Node.js ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
pnpm-lock.yaml

# ── 🔐 Environment Config ──
.env
.env.*
!.env.example              # Keep template visible

# ── 💾 Platform-Specific ──
.DS_Store
Thumbs.db
*.lnk

# ── 📱 Session & Auth Data ──
session/
src/session/
src/auth/
src/creds/
**/creds.json
**/keys/
src/session_backups/

# ── 🗃️ Database / Cache ──
src/database/database.json
src/database/baileys_store.json
src/database/miara_cache.db
src/database/*.sqlite
src/database/*.db

# ── 📜 Logs & Temporary ──
logs/
*.log
*.tmp
*.bak
*.swp
tmp/
temp/
*.pid

# ── 🪣 Backups ──
backup/
*.zip
*.tar.gz
*.bak.json

# ── ⚙️ Build / Runtime Artifacts ──
dist/
build/
coverage/
.cache/
*.map

# ── 🧠 Models & AI Assets ──
models/
*.h5
*.onnx
*.bin
*.weights
*.model.json

# ── 🌐 Platform Deployment ──
.vercel/
.render/
.heroku/
railway.json

# ── 🧰 Editors & IDEs ──
.vscode/
.idea/
*.code-workspace
*.iml

# ── 💡 Miscellaneous ──
test-results/
.env.bak
*.orig
